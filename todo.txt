- 文本编辑和预览同步进行
    当前并没有十分必要。
    如果要做可以通过 websocket 发送最近的章节的 ID 给客户端。然后让客户端滚动到
    对应的位置即可。不过可能闪烁严重，并没有十分必要。
    章节 ID 可以是隐式的，或者是显示定义的锚点。

- 浏览器的缓存可能影响预览效果。建议每次新加载文件后都运行下 `Ctrl+Shift+R` 清理缓存。

- 需要把这套流程打造成自动化构建之类的东西，无论是基于web的还是本地的或者是基于容器的。

